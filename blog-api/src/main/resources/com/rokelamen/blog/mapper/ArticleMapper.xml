<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rokelamen.blog.mapper.ArticleMapper">

    <!--List<Archives> listArchives();-->
    <select id="listArchives" resultType="com.rokelamen.blog.dos.Archives">
        select year(from_unixtime(create_date/1000, '%Y-%m-%d %h:%i:%s')) as year, month(from_unixtime(create_date/10000, '%Y-%m-%d %h:%i:%s')) as month, count(*) as count
        from ms_article group by year, month
    </select>
</mapper>